name: Java CI with Maven

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  Build-Test-Package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up openjdk-15
        uses: actions/setup-java@v1
        with:
          java-version: 15.0.1
      - name: Build with Maven
        run: Containers/request-service/mvn -B package --file pom.xml
        run: Containers/response-service/mvn -B package --file pom.xml
